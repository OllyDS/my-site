import { stylesheet } from 'typestyle';

const style = stylesheet({
    background: {
        backgroundImage: 'linear-gradient(white, grey)',
        height: '100%',
        width: '100%',
    },
});

export { style };
