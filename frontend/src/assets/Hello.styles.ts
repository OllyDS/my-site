import { stylesheet } from 'typestyle';

const style = stylesheet({
    title: {
        color: 'darkorange',
        fontFamily: 'Rubik',
        fontSize: 96,
        fontWeight: 700,
        margin: 0,
        paddingTop: 36,
        textAlign: 'center',
        textShadow: '2px 0 black',
    },
});

export { style };
